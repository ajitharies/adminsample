import React from 'react'

const Orders = () => {
  return (
    <div>
      <button className="order-btn" onClick={() => Orders()}>
        Orders
      </button>
    </div>
  )
}

export default Orders
